///
/// HybridMultipleImagePickerSpecCxx.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2024 Marc Rousavy @ Margelo
///

import Foundation
import NitroModules

/**
 * Helper class for converting instances of `HybridMultipleImagePickerSpecCxx` from- and to unsafe pointers.
 * This is useful to pass Swift classes to C++, without having to strongly type the C++ function signature.
 * The actual Swift type can be included in the .cpp file, without having to forward-declare anything in .hpp.
 */
public final class HybridMultipleImagePickerSpecCxxUnsafe {
  /**
   * Casts a `HybridMultipleImagePickerSpecCxx` instance to a retained unsafe raw pointer.
   * This acquires one additional strong reference on the object!
   */
  public static func toUnsafe(_ instance: HybridMultipleImagePickerSpecCxx) -> UnsafeMutableRawPointer {
    return Unmanaged.passRetained(instance).toOpaque()
  }

  /**
   * Casts an unsafe pointer to a `HybridMultipleImagePickerSpecCxx`.
   * The pointer has to be a retained opaque `Unmanaged<HybridMultipleImagePickerSpecCxx>`.
   * This removes one strong reference from the object!
   */
  public static func fromUnsafe(_ pointer: UnsafeMutableRawPointer) -> HybridMultipleImagePickerSpecCxx {
    return Unmanaged<HybridMultipleImagePickerSpecCxx>.fromOpaque(pointer).takeRetainedValue()
  }
}

/**
 * A class implementation that bridges HybridMultipleImagePickerSpec over to C++.
 * In C++, we cannot use Swift protocols - so we need to wrap it in a class to make it strongly defined.
 *
 * Also, some Swift types need to be bridged with special handling:
 * - Enums need to be wrapped in Structs, otherwise they cannot be accessed bi-directionally (Swift bug: https://github.com/swiftlang/swift/issues/75330)
 * - Other HybridObjects need to be wrapped/unwrapped from the Swift TCxx wrapper
 * - Throwing methods need to be wrapped with a Result<T, Error> type, as exceptions cannot be propagated to C++
 */
public class HybridMultipleImagePickerSpecCxx {
  /**
   * The Swift <> C++ bridge's namespace (`margelo::nitro::multipleimagepicker::bridge::swift`)
   * from `MultipleImagePicker-Swift-Cxx-Bridge.hpp`.
   * This contains specialized C++ templates, and C++ helper functions that can be accessed from Swift.
   */
  public typealias bridge = margelo.nitro.multipleimagepicker.bridge.swift

  /**
   * Holds an instance of the `HybridMultipleImagePickerSpec` Swift protocol.
   */
  private var __implementation: any HybridMultipleImagePickerSpec

  /**
   * Create a new `HybridMultipleImagePickerSpecCxx` that wraps the given `HybridMultipleImagePickerSpec`.
   * All properties and methods bridge to C++ types.
   */
  public init(_ implementation: some HybridMultipleImagePickerSpec) {
    self.__implementation = implementation
    /* no base class */
  }

  /**
   * Get the actual `HybridMultipleImagePickerSpec` instance this class wraps.
   */
  @inline(__always)
  public func getHybridMultipleImagePickerSpec() -> any HybridMultipleImagePickerSpec {
    return __implementation
  }

  /**
   * Contains a (weak) reference to the C++ HybridObject to cache it.
   */
  public var hybridContext: margelo.nitro.HybridContext {
    @inline(__always)
    get {
      return self.__implementation.hybridContext
    }
    @inline(__always)
    set {
      self.__implementation.hybridContext = newValue
    }
  }

  /**
   * Get the memory size of the Swift class (plus size of any other allocations)
   * so the JS VM can properly track it and garbage-collect the JS object if needed.
   */
  @inline(__always)
  public var memorySize: Int {
    return self.__implementation.memorySize
  }

  // Properties
  

  // Methods
  @inline(__always)
  public func openPicker(config: NitroConfig, resolved: bridge.Func_void_std__vector_Result_, rejected: bridge.Func_void_double) -> Void {
    do {
      try self.__implementation.openPicker(config: config, resolved: { () -> (([Result]) -> Void) in
        let __sharedClosure = bridge.share_Func_void_std__vector_Result_(resolved)
        return { (__result: [Result]) -> Void in
          __sharedClosure.pointee.call({ () -> bridge.std__vector_Result_ in
          var __vector = bridge.create_std__vector_Result_(__result.count)
          for __item in __result {
            __vector.push_back(__item)
          }
          return __vector
        }())
        }
      }(), rejected: { () -> ((Double) -> Void) in
        let __sharedClosure = bridge.share_Func_void_double(rejected)
        return { (__reject: Double) -> Void in
          __sharedClosure.pointee.call(__reject)
        }
      }())
      return 
    } catch {
      let __message = "\(error.localizedDescription)"
      fatalError("Swift errors can currently not be propagated to C++! See https://github.com/swiftlang/swift/issues/75290 (Error: \(__message))")
    }
  }
  
  @inline(__always)
  public func openCrop(image: std.string, config: NitroCropConfig, resolved: bridge.Func_void_CropResult, rejected: bridge.Func_void_double) -> Void {
    do {
      try self.__implementation.openCrop(image: String(image), config: config, resolved: { () -> ((CropResult) -> Void) in
        let __sharedClosure = bridge.share_Func_void_CropResult(resolved)
        return { (__result: CropResult) -> Void in
          __sharedClosure.pointee.call(__result)
        }
      }(), rejected: { () -> ((Double) -> Void) in
        let __sharedClosure = bridge.share_Func_void_double(rejected)
        return { (__reject: Double) -> Void in
          __sharedClosure.pointee.call(__reject)
        }
      }())
      return 
    } catch {
      let __message = "\(error.localizedDescription)"
      fatalError("Swift errors can currently not be propagated to C++! See https://github.com/swiftlang/swift/issues/75290 (Error: \(__message))")
    }
  }
  
  @inline(__always)
  public func openPreview(media: bridge.std__vector_MediaPreview_, index: Double, config: NitroPreviewConfig) -> Void {
    do {
      try self.__implementation.openPreview(media: media.map({ __item in __item }), index: index, config: config)
      return 
    } catch {
      let __message = "\(error.localizedDescription)"
      fatalError("Swift errors can currently not be propagated to C++! See https://github.com/swiftlang/swift/issues/75290 (Error: \(__message))")
    }
  }
  
  @inline(__always)
  public func openCamera(config: NitroCameraConfig, resolved: bridge.Func_void_CameraResult, rejected: bridge.Func_void_double) -> Void {
    do {
      try self.__implementation.openCamera(config: config, resolved: { () -> ((CameraResult) -> Void) in
        let __sharedClosure = bridge.share_Func_void_CameraResult(resolved)
        return { (__result: CameraResult) -> Void in
          __sharedClosure.pointee.call(__result)
        }
      }(), rejected: { () -> ((Double) -> Void) in
        let __sharedClosure = bridge.share_Func_void_double(rejected)
        return { (__reject: Double) -> Void in
          __sharedClosure.pointee.call(__reject)
        }
      }())
      return 
    } catch {
      let __message = "\(error.localizedDescription)"
      fatalError("Swift errors can currently not be propagated to C++! See https://github.com/swiftlang/swift/issues/75290 (Error: \(__message))")
    }
  }
}
